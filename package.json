{
  "name": "harcama-takip",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler pages dev",
    "deploy": "wrangler pages deploy",
    "db:init:local": "wrangler d1 execute harcama-takip-db --local --file=schema.sql",
    "db:init:remote": "wrangler d1 execute harcama-takip-db --remote --file=schema.sql",
    "db:migrate:002:local": "wrangler d1 execute harcama-takip-db --local --file=migrations/002_user_display_name.sql",
    "db:migrate:002:remote": "wrangler d1 execute harcama-takip-db --remote --file=migrations/002_user_display_name.sql"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
